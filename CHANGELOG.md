# Changelog

## v3

### Added
- Multi-agent `Scheduler` that runs agents concurrently using threads
- `Task` abstraction with parameter dictionaries
- Built-in logging for task lifecycle events

### Notes
- The included website highlights v3 and provides an interactive, in-browser scheduler simulation.
